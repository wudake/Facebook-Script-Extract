# 复制为 .env.local 后填入实际凭据；.env.local 已被 .gitignore 忽略
# 这些变量在 Vite 构建期内联到 JS bundle，仅作前端轻量门控，不是后端鉴权

VITE_ADMIN_USERNAME=your-admin-username
VITE_ADMIN_PASSWORD=your-strong-password

# 可选：跨域部署时指向后端绝对地址（如 https://api.example.com）
# 同域 Nginx 反代部署时留空，由 /api 与 /ws 走相对路径
# VITE_API_BASE_URL=
